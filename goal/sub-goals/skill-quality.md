# Skill Quality

Each skill should read as a focused behavior contract: clear activation
surface, narrow scope, enough direction to make the first useful move, explicit
boundaries, and no more structure than its behavior genuinely needs. Skill
updates should generalize durable behavior instead of encoding one-off session
details, and should remain easy for a future agent to load, scan, and follow.
